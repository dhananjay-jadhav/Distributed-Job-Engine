{
  "name": "proto",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/proto/src",
  "projectType": "library",
  "tags": [],
  "// targets": "to see all targets run: nx show project proto --web",
  "targets": {
    "build": {
      "dependsOn": ["gen:ts-proto"]
    },
    "gen:ts-proto": {
      "command": "yarn gen:proto-types"
    }
  }
}
